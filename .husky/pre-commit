if git diff --staged --quiet backend/; then
  echo "No changes in backend directory, skipping backend build"
  exit 0
fi

echo "Building backend"
cd backend && pnpm run ts.check && pnpm run build && git add dist